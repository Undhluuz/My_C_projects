<Window x:Class="SimpleAccounting.AddCounterpartyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add/Edit Counterparty" Height="450" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Content="Name:" Grid.Row="0" />
        <TextBox Text="{Binding NewCounterparty.Name}" Grid.Row="1" Margin="0,0,0,10" />

        <Label Content="Tax ID:" Grid.Row="2" />
        <TextBox Text="{Binding NewCounterparty.TaxId}" Grid.Row="3" Margin="0,0,0,10" />

        <Label Content="Operations:" Grid.Row="4" />
        <DataGrid Grid.Row="5" ItemsSource="{Binding Operations}" AutoGenerateColumns="False" IsReadOnly="True" Height="150">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="*" />
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="*" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add Operation" Command="{Binding AddOperationCommand}" Margin="5" />
            <Button Content="OK" Command="{Binding AddCommand}" Margin="5" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5" />
        </StackPanel>
    </Grid>
</Window>